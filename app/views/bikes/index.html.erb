<h1>Bikes</h1>

<% if @bikes.any? %>
  <table class="table table-striped">
    <thead>
      <tr>
        <th>Model</th>
        <th>Serial Number</th>
        <th>Owner</th>
      </tr>
    </thead>
    <tbody>
      <%= render @bikes %>
    </tbody>
  </table>
<% else %>
  <p>No bikes yet.</p>
<% end %>